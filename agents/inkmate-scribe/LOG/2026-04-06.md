# LOG — 2026-04-06

- **Date**: 2026-04-06
- **Action**: Fix Chat Tool Loop Error (Groq HarmonyError)
- **Result**: Success
- **Details**:
  - Refactored `src/app/api/chat/route.ts` from `ToolLoopAgent` to `streamText` + `stopWhen: stepCountIs(5)`.
  - Replaced `convertToCoreMessages` (hallucination) with `convertToModelMessages` (correct SDK v6 export).
  - Fixed `UIMessage` rendering in `chats/page.tsx` to handle `parts` for user messages, fixing the "empty bubble" issue.
  - Corrected tool-part mapping in `route.ts` to prevent corruption of tool names that caused Groq's "Tools should have a name!" error.
  - Implemented `experimental_wrapLanguageModel` with middleware to ensure `toolName` presence.
  - Added conversion of `reasoning` parts to `text` parts in the middleware to resolve Step 3 errors (Groq Harmony incompatibility with reasoning parts in history).
  - Implemented an "Ultra-Clean" middleware in `route.ts` that:
    - Strips empty text parts (e.g., `{ type: 'text', text: '' }`) from assistant history.
    - Removes `providerOptions` and all non-standard metadata from prompt parts.
    - Converts `reasoning` parts to `text`.
    - Merges consecutive text parts.
    - Strictly maps to standard `LanguageModelV3` fields, excluding `includeRawChunks` and `undefined` fields.
